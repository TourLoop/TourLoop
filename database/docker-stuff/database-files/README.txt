Tourloop's data was obtained from the OpenStreetMap project under the Open Data Commons Open Database License (ODbL).
Thus, TourLoop's data must also comply to the ODbL.
The CSV files in this tar file are the datafiles used to create the TourLoop Neo4j database.
TourLoop uses an admin import into Neo4j to convert the CSV files into a Neo4j database.
